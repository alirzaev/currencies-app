<resources>
    <!-- Base application theme. -->
    <style name="Theme.Currencies" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange_500</item>
        <item name="colorPrimaryVariant">@color/orange_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/amber_100</item>
        <item name="colorSecondaryVariant">@color/amber_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/dark_grey</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Splash screen theme. -->
    <style name="Theme.Currencies.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_splashscreen</item>
        <item name="windowSplashScreenBackground">@color/dark_grey</item>
        <item name="postSplashScreenTheme">@style/Theme.Currencies.NoActionBar</item>
    </style>
</resources>